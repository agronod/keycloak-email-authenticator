# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Determine Build Profile
        id: set-profile
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            # Use the target branch for PRs
            TARGET_BRANCH="${{ github.base_ref }}"
          else
            # Use the current branch for normal commits
            TARGET_BRANCH="${{ github.ref_name }}"
          fi

          if [[ "$TARGET_BRANCH" == "main" ]]; then
            echo "PROFILE=prod" >> $GITHUB_ENV
          elif [[ "$TARGET_BRANCH" == "develop" ]]; then
            echo "PROFILE=dev" >> $GITHUB_ENV
          else
            echo "PROFILE=dev" >> $GITHUB_ENV  # Default to dev for other branches
          fi

      - name: Build with Maven
        run: mvn -P${{ env.PROFILE }} -B package --file pom.xml

      - uses: actions/upload-artifact@v4
        with:
          name: keycklok-email-authentication-jar-with-dependencies
          path: target/keycklok-email-authentication-jar-with-dependencies.jar

  create_github_release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: keycklok-email-authentication-jar-with-dependencies
          path: downloaded-artifact

      - run: mkdir jars

      - run: mv downloaded-artifact/keycklok-email-authentication-jar-with-dependencies.jar jars/keycklok-email-authentication-jar-with-dependencies.jar

      - name: Determine Release or Pre-Release
        id: set_release_type
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "PRERELEASE=true" >> $GITHUB_ENV
            echo "PRE_RELEASE_BRANCHES=${{ github.base_ref }}" >> $GITHUB_ENV
            echo "APPEND_TAG=-${{ github.base_ref }}-pr-${{ github.event.pull_request.number }}" >> $GITHUB_ENV
          else
            echo "PRERELEASE=false" >> $GITHUB_ENV
            echo "PRE_RELEASE_BRANCHES=" >> $GITHUB_ENV
            echo "APPEND_TAG=" >> $GITHUB_ENV
          fi

      - uses: mathieudutour/github-tag-action@v6.0
        id: tag_version
        with:
          pre_release_branches: ${{ env.PRE_RELEASE_BRANCHES }}
          append_to_pre_release_tag: ${{ env.APPEND_TAG }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}
          files: |
            jars/keycklok-email-authentication-jar-with-dependencies.jar
          prerelease: ${{ env.PRERELEASE }}
        env:
          github_token: ${{ secrets.GITHUB_TOKEN }}
